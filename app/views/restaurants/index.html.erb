<h1 class="text-center">All restaurants</h1>
<h2 class="text-center"><%= link_to "ajouter un resto", new_restaurant_path(), class: 'btn btn-success'%></h2>

<div class="container">
  <hr/>
  <div class="row">
    <% @restaurants.each do |restaurant| %>
    <div class="col-xs-12 col-sm-4">
      <h2><%= restaurant.name%></h2>
      <p class="badge"><%= restaurant.category%></p>
      <p><%= restaurant.address%></p>
      <%= link_to "+ de détails", restaurant_path(restaurant), class: 'btn btn-primary'%>
      <%= link_to "ajouter une review", new_restaurant_review_path(restaurant), class: 'btn btn-primary'%>
    </div>
    <% end %>
  </div>
</div>




